# PRD: 修复 /dev 文档中的循环机制说明

## 背景

skills/dev/SKILL.md 文档中关于循环机制的说明有误导：
- 说"必须在 Ralph Loop 内调用"（指 plugin）
- 说"不要直接调用 /dev"
- 但无头模式就是直接调用 `claude -p "/dev"`

实际情况：
- 有头模式：使用 /ralph-loop plugin（dev-with-loop 调用）
- 无头模式：使用 cecelia-run while 循环（直接 claude -p "/dev"）

需要更新文档，删除 Stop Hook 说明，统一为"循环机制"。

## 目标

修改 skills/dev/SKILL.md：
1. 删除 Stop Hook 相关说明
2. 统一为"循环机制"概念
3. 明确两种实现：
   - 有头：/ralph-loop plugin
   - 无头：cecelia-run while 循环

## 修改范围

**skills/dev/SKILL.md**：
- 第 6 行 description：改为"必须在循环机制内调用"
- 第 23-51 行：删除 Stop Hook 说明，改为循环机制说明
- 第 55-76 行：删除 Stop Hook 相关内容

## 成功标准

1. 文档不再提及 Stop Hook
2. 文档明确说明有头/无头两种循环方式
3. 无矛盾：无头用 `claude -p "/dev"` 是正确的
4. pr-gate 降级为提示型（exit 0，不阻断）
