# Step 8: 提交 PR

> 版本号 + commit + push + PR
> **p0 阶段在此结束：PR 创建后立即结束对话，不等待 CI**

---

## 8.1 会话恢复检测

**先检测是否是恢复的会话**：

```bash
EXISTING_PR=$(gh pr list --head "$BRANCH_NAME" --state all --json number,url,state -q '.[0]' 2>/dev/null)

if [ ! -z "$EXISTING_PR" ]; then
  PR_STATE=$(echo "$EXISTING_PR" | jq -r '.state')

  if [ "$PR_STATE" = "MERGED" ]; then
    echo "✅ PR 已合并，跳到 cleanup"
  elif [ "$PR_STATE" = "OPEN" ]; then
    echo "✅ PR 已存在，跳到 CI"
  fi
fi
```

---

## 8.2 版本号更新

**检测是否为测试任务**：

```bash
IS_TEST=$(git config branch."$BRANCH_NAME".is-test 2>/dev/null)

if [ "$IS_TEST" = "true" ]; then
    echo "🧪 测试任务：跳过 CHANGELOG 和版本号更新"
    # 不更新 package.json 和 CHANGELOG.md
else
    # 正常更新版本号
fi
```

**正常任务：提交前必须更新版本号（semver）**：

| commit 类型 | 版本变化 |
|-------------|----------|
| fix: | patch (+0.0.1) |
| feat: | minor (+0.1.0) |
| feat!: / BREAKING: | major (+1.0.0) |

**版本号变化示例**：

| 当前版本 | commit 类型 | 新版本 |
|---------|-------------|--------|
| 8.16.0 | fix: | 8.16.1 |
| 8.16.1 | feat: | 8.17.0 |
| 8.17.0 | feat!: | 9.0.0 |

更新 `package.json` 和 `CHANGELOG.md`。

**测试任务**：跳过版本号和 CHANGELOG 更新，commit 消息使用 `test:` 前缀。

---

## 8.3 Squash Evidence Commit（新增）

**在提交前，检查并合并 evidence commit**：

```bash
# 如果最后一个 commit 是 evidence commit，自动合并到代码 commit
bash scripts/squash-evidence.sh
```

**说明**：
- 避免 SHA 不匹配问题
- 保持干净的 commit 历史
- 最终只有一个 commit（代码 + evidence）

---

## 8.4 提交

```bash
git add -A
git commit -m "feat: <功能描述>

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

**注意**：
- 如果 Step 7 已经暂存了 evidence，这里会一次性提交
- 如果没有暂存 evidence，这里会包含所有变更

---

## 8.5 推送

```bash
/usr/bin/git push -u origin HEAD
```

---

## 8.5 创建 PR

```bash
gh pr create --base develop --title "feat: <功能描述>" --body "## Summary
- <主要改动>

## Test
- [x] 本地测试通过

---
Generated by /dev workflow"
```

---

## 8.6 p0 阶段完成

**Stop Hook 行为**：

- PR 创建后，Stop Hook 检测到 PHASE=p0
- p0 阶段只检查：质检通过 + PR 创建成功
- **不检查 CI 状态**（p0 不等待 CI）
- Stop Hook 返回 `exit 0`，允许会话结束

**两阶段分离**：

```
p0 (Published): 质检 → PR → 结束（不等 CI）
p1 (CI fail):   修复 → push → 结束（等下次唤醒）
```

**下一步**：

- CI fail 时，通过通知或手动启动进入 p1 阶段（Step 9）
- CI pass 时，GitHub 自动合并，直接进入 Learning/Cleanup
