# L2B Evidence: cp-remove-pr-gate-hook

## 验证命令

```bash
# 检查 settings.json 中是否还有 pr-gate-v2.sh
grep -c "pr-gate-v2.sh" .claude/settings.json
# 结果: 0 (已移除)

# 检查 branch-protect.sh 仍在配置中
grep -c "branch-protect.sh" .claude/settings.json
# 结果: 1 (保留)
```

## 手动验证

- settings-check: pr-gate-v2.sh 已从 hooks 配置中移除
- branch-protect-check: branch-protect.sh 仍然配置

## 自动化测试

```bash
npm run test
# CI 运行验证
```

## 机器引用

- Branch: cp-remove-pr-gate-hook
- Test: CI 验证
