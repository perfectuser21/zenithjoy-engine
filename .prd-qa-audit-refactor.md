# PRD: 清理 zenithjoy-engine 过期配置和文档

## 功能描述

清理项目中不再使用的配置、Hook 和 Cecelia 相关文档，保持代码库整洁。

## 需求来源

系统检查发现多个过期配置：
1. git-push-and-wait 规则从未使用
2. session-start.sh Hook 已废弃
3. AI Thinking 限制规则需要移除
4. Cecelia 相关文档应该移到 Cecelia workspace

## 成功标准

### 1. 移除全局 CLAUDE.md 中的过期规则

- 删除 "AI Thinking 规则（覆盖系统默认）" 整个章节
- 删除 "Git Push + CI 监控" 章节中的 git-push-and-wait 强制使用规则（保留手动模式说明）

### 2. 删除 session-start.sh Hook

- 从 `.claude/settings.json` 移除 SessionStart hook 配置
- 删除 `hooks/session-start.sh` 文件

### 3. 移动 Cecelia 相关文档

将以下文件移动到 `/home/xx/dev/cecelia-workspace/docs/from-engine/`：

- `docs/runbooks/DRCA-v2.md`
- `docs/paths/GOLDEN-PATHS.md` 中的 N1 部分（提取出来）
- `docs/paths/OPTIMAL-PATHS.md` 中的 N1 部分（提取出来）
- `docs/paths/MINIMAL-PATHS.md` 中的 N1 部分（提取出来）
- `docs/TEST-CECELIA.md`
- `docs/PHASE-OVERRIDE.md`
- `docs/CONTRACT-REBASE-ACCEPTANCE.md`
- `docs/HOOK-CROSS-REPO-ISSUE.md`
- `docs/INTERFACE-SPEC.md` 中的 Cecelia 部分（提取出来）

注意：
- 只移动 Cecelia 相关内容，不要删除整个文件
- 如果文件中有非 Cecelia 内容，只提取 Cecelia 部分
- 在 cecelia-workspace 创建 `docs/from-engine/` 目录

### 4. 删除 git-push-and-wait 脚本（可选）

- 确认 `/home/xx/bin/git-push-and-wait` 是否被其他项目使用
- 如果未使用，删除该脚本

## 验收清单

- 全局 CLAUDE.md 中不再有 AI Thinking 限制和 git-push-and-wait 强制规则
- `.claude/settings.json` 中没有 SessionStart hook
- `hooks/session-start.sh` 不存在
- Cecelia 相关文档已移到 cecelia-workspace
- zenithjoy-engine 中的文档不再包含 Cecelia 引用
- CI 通过
